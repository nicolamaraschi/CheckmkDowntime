# ============================================
# CHECKMK DOWNTIME - Environment Variables
# ============================================
# IMPORTANTE: Questo è un template. 
# In Portainer, configura TUTTE queste variabili nella sezione "Environment Variables"

# === CHECKMK ===
CHECKMK_HOST=checkmk.tuodominio.com
CHECKMK_SITE=monitoring
CHECKMK_USER=automation
CHECKMK_PASSWORD=your-automation-password

# === AWS COGNITO (Autenticazione) ===
COGNITO_REGION=eu-west-1
COGNITO_USER_POOL_ID=eu-west-1_XXXXXXXXX
COGNITO_APP_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxx

# === AWS ATHENA (CloudConnexa Dashboard) ===
ATHENA_DB=cloudconnexa_logs_db
ATHENA_RESULTS_BUCKET=s3://bucket-athena-results/
ATHENA_WORKGROUP=primary
AWS_REGION=eu-central-1

# === AWS ATHENA (SAP Dashboard) ===
SAP_ATHENA_DB=sap_reports_db
SAP_ATHENA_WORKGROUP=ReportCheckSistemiSap

# === AWS CREDENTIALS (Solo se NON usi IAM Role) ===
# Se il container gira su EC2 con IAM Role, commenta queste righe
AWS_ACCESS_KEY_ID=AKIAXXXXXXXXXXXXXXXX
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# === CONTAINER (Opzionali) ===
BACKEND_PORT=8000
NGINX_PORT=80

# === PORTAINER MAPPING ===
# Porta esterna su cui esporre l'app
# Esempio: 8436:80 (mappa la porta 80 del container alla 8436 dell'host)
EXTERNAL_PORT=8436
